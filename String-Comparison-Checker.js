var a = prompt("Enter the first string : ")
var b = prompt("Enter the second string : ")

if (a==b){
    document.write("The strings entered are identical.")
}
else{
    document.write("The strings entered are different.")
}
